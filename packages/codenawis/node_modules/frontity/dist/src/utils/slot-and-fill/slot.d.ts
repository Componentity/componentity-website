import React from "react";
import { Connect, Package } from "../../../types";
interface SlotProps {
    name: string;
    data?: any;
    [key: string]: any;
}
declare const _default: React.FunctionComponent<Pick<Connect<Package, SlotProps>, string | number>>;
export default _default;
